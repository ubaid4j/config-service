apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-service
  labels:
    app: config-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config-service
  template:
    metadata:
      labels:
        app: config-service
    spec:
      containers:
        - name: config-service
          image: ghcr.io/ubaid4j/config-service:0.0.1-snapshot
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50100
          env:
            - name: SERVER_PORT
              value: "50100"
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 5"]
